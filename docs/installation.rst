============
Installation
============

At the command line::

    $ easy_install django-simple-sql-debug

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-simple-sql-debug
    $ pip install django-simple-sql-debug
